# Orbital Environment Configuration for Docker

APP_NAME=Orbital
APP_ENV=production
APP_URL=http://localhost:8058

# Server Configuration
PORT=8058
HOST=0.0.0.0

# Redis Configuration
REDIS_URL=redis://redis:6379
REDIS_PORT=6379

# Database (SQLite) - Docker uses volume mount
DB_DATABASE=/app/database/database.sqlite

# API Keys (comma-separated for multiple keys)
# IMPORTANT: Change this to a secure random key in production!
API_KEYS=your-secret-api-key-change-me

# Storage Paths
STORAGE_PATH=/app/storage/app/artifacts

# Worker Configuration
WORKER_REPLICAS=2
